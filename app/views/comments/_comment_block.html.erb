<legend>Comments</legend>

<table class='table'>
  <tbody>
    <% @comments.each do |c| %>
      <tr>
        <td width='20%'><strong><%= c.name.blank? ? 'Human' : c.name %></strong><br><%= c.created_at %></td>
        <td><%= c.body %></td>
      </tr>
    <% end %>
  </tbody>
</table>
